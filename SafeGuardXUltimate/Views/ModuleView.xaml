<UserControl x:Class="SafeGuardXUltimate.Views.ModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource HeadingTextStyle}" />
        <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="{DynamicResource TextSecondary}" Margin="0,8,0,16" />

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <StackPanel>
                <CheckBox Content="Module enabled" IsChecked="{Binding IsEnabled}" />
                <TextBlock Text="Sensitivity" Margin="0,8,0,0" />
                <Slider Minimum="1" Maximum="100" Value="{Binding Sensitivity}" />
                <Button Content="Run simulation" Command="{Binding SimulateCommand}" Style="{StaticResource ActionButtonStyle}" Width="180" Margin="0,16,0,0" />
                <TextBlock Text="{Binding Status}" Margin="0,10,0,0" Foreground="{DynamicResource AccentPrimary}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
